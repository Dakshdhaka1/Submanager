version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - FRONTEND_URL=http://localhost

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
